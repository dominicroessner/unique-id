# unique-id
Generate a unique id to be shared across jobs in a GitHub Actions workflow.

Uses the `jobs.<job_id>.outputs` GitHub Actions syntax as documented [here](https://docs.github.com/en/free-pro-team@latest/actions/reference/workflow-syntax-for-github-actions#jobsjob_idoutputs).
